name: Build Manager Images

on:
  push:
    paths:
      - '../../deskcli/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache-dependency-path: deskcli/go.sum

      - name: Tidy dependencies
        working-directory: deskcli
        run: go mod tidy

      - name: Build
        working-directory: deskcli
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: go build -ldflags="-s -w" -o ../dist/deskcli-${{ matrix.goos }}-${{ matrix.goarch }} .

      - uses: actions/upload-artifact@v4
        with:
          name: deskcli-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: deskcli-*
          merge-multiple: true
          path: dist/

      - name: Publish Beta Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v0.0.0-beta
          name: Beta Build
          prerelease: true
          files: dist/deskcli-*
