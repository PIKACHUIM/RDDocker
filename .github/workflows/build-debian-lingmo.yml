name: Build Debian - lingmo

on:
  workflow_call:
  workflow_dispatch:

env:
  IMAGE_PREFIX: pikachuim/debian

jobs:
  build:
    if: false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/docker-login
        with:
          username: ${{ secrets.DOCKER_PUBHUB_USER }}
          password: ${{ secrets.DOCKER_PUBHUB_PASS }}
          ghcr_token: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: .
          file: dockers/debian/desktops/lingmo
          platforms: linux/amd64
          push: true
          tags: |
            ${{ env.IMAGE_PREFIX }}:12-lingmo
            ${{ env.IMAGE_PREFIX }}:bookworm-lingmo
            ghcr.io/${{ env.IMAGE_PREFIX }}:12-lingmo
            ghcr.io/${{ env.IMAGE_PREFIX }}:bookworm-lingmo
          build-args: |
            OS_VERSION=bookworm
            OS_SYSTEMS=debian
            OS_VERSHOW=12
          cache-from: type=gha
          cache-to: type=gha,mode=max
